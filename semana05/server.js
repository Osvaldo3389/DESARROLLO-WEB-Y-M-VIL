// se ocupan los modulos por defecto
const express = require('express');
const mongoose = require('mongoose');
const cors = require('cors'); // el cors valida los certificados de seguridad de los navegadores
